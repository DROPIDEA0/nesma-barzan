# دليل استخدام قواعد البيانات - نسمة برزان

## 🎯 المشكلة التي واجهتها

حاولت استيراد ملف **SQLite** (`local.db`) إلى **MySQL/MariaDB** عبر phpMyAdmin، وهذا لا يعمل لأن:
- SQLite و MySQL صيغتان مختلفتان تماماً
- phpMyAdmin مخصص فقط لـ MySQL/MariaDB
- لا يمكن استيراد SQLite مباشرة إلى MySQL

---

## ✅ الحلول المتاحة

### الحل 1: استخدام SQLite (موصى به للتطوير المحلي)

**المميزات:**
- ✅ لا يحتاج خادم قاعدة بيانات منفصل
- ✅ ملف واحد سهل النقل
- ✅ مثالي للتطوير المحلي
- ✅ البيانات موجودة بالفعل

**كيفية الاستخدام:**
1. ضع ملف `local.db` في مجلد المشروع
2. المشروع سيستخدمه تلقائياً
3. لا حاجة لأي إعدادات إضافية

**لعرض البيانات:**
- استخدم **DB Browser for SQLite**: https://sqlitebrowser.org/
- أو **SQLiteStudio**: https://sqlitestudio.pl/

---

### الحل 2: استخدام MySQL (للإنتاج)

**المميزات:**
- ✅ أفضل للمواقع الكبيرة
- ✅ يدعم عدة مستخدمين في نفس الوقت
- ✅ متوافق مع معظم خدمات الاستضافة

**كيفية الاستخدام:**

#### الخطوة 1: إنشاء قاعدة بيانات جديدة
1. افتح phpMyAdmin
2. اضغط على "New" لإنشاء قاعدة بيانات جديدة
3. اسم القاعدة: `nesma_barzan`
4. Collation: `utf8mb4_unicode_ci`

#### الخطوة 2: استيراد الملف
1. اختر قاعدة البيانات `nesma_barzan`
2. اذهب إلى تبويب "Import"
3. اختر ملف `nesma-barzan-mysql.sql`
4. اضغط "Go"

#### الخطوة 3: تحديث ملف .env
```env
# غيّر من SQLite إلى MySQL
DATABASE_URL=mysql://username:password@localhost:3306/nesma_barzan

# مثال:
DATABASE_URL=mysql://root:@localhost:3306/nesma_barzan
```

#### الخطوة 4: تحديث كود المشروع
في ملف `server/db.ts`، غيّر من:
```typescript
import Database from 'better-sqlite3';
const db = new Database('local.db');
```

إلى:
```typescript
import { drizzle } from 'drizzle-orm/mysql2';
import mysql from 'mysql2/promise';

const connection = await mysql.createConnection(process.env.DATABASE_URL);
export const db = drizzle(connection);
```

---

## 📁 الملفات المتوفرة

### 1. `local.db` - قاعدة بيانات SQLite
- **الاستخدام:** للتطوير المحلي
- **البرنامج:** DB Browser for SQLite
- **الحالة:** ✅ جاهزة ومليئة بالبيانات

### 2. `nesma-barzan-mysql.sql` - ملف SQL لـ MySQL
- **الاستخدام:** للإنتاج أو إذا كنت تفضل MySQL
- **البرنامج:** phpMyAdmin
- **الحالة:** ✅ جاهز للاستيراد

### 3. `.env` - ملف المتغيرات
- **الحالة الحالية:** مُعد لـ SQLite
- **للتغيير:** عدّل `DATABASE_URL` إذا أردت استخدام MySQL

---

## 🔍 كيفية عرض البيانات

### لـ SQLite (local.db):

#### الطريقة 1: DB Browser for SQLite (الأسهل)
1. حمّل البرنامج من: https://sqlitebrowser.org/
2. افتح ملف `local.db`
3. اذهب إلى تبويب "Browse Data"
4. اختر جدول `site_settings`
5. ستشاهد جميع البيانات!

#### الطريقة 2: SQLiteStudio
1. حمّل من: https://sqlitestudio.pl/
2. افتح ملف `local.db`
3. استعرض الجداول

### لـ MySQL (بعد الاستيراد):

1. افتح phpMyAdmin
2. اختر قاعدة البيانات `nesma_barzan`
3. اضغط على جدول `site_settings`
4. ستشاهد جميع البيانات!

---

## 📊 البيانات الموجودة

### جدول site_settings (9 إعدادات)

| المفتاح | القيمة |
|---------|--------|
| site_logo | /uploads/logo.png |
| site_name_ar | نسمة برزان التجارية |
| site_name_en | Nesma Barzan Trading |
| foundation_year | 2005 |
| contact_phone | +966 555 499 991 |
| contact_email | info@shheer.com |
| contact_website | www.shheer.com |
| contact_address_ar | الرياض، المملكة العربية السعودية |
| contact_address_en | Riyadh, Saudi Arabia |

---

## ⚠️ ملاحظات مهمة

### للتطوير المحلي:
- ✅ استخدم **SQLite** (local.db)
- ✅ لا حاجة لتغيير أي شيء
- ✅ المشروع يعمل مباشرة

### للإنتاج (الاستضافة):
- ✅ استخدم **MySQL** (nesma-barzan-mysql.sql)
- ✅ غيّر `DATABASE_URL` في ملف .env
- ✅ قد تحتاج لتعديل كود الاتصال بقاعدة البيانات

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "البيانات لا تظهر"
**الحل:**
- تأكد من فتح الجدول الصحيح (`site_settings`)
- تأكد من أن البرنامج يدعم UTF-8 للنصوص العربية

### المشكلة: "خطأ في الاستيراد إلى MySQL"
**الحل:**
- استخدم ملف `nesma-barzan-mysql.sql` وليس `local.db`
- تأكد من أن قاعدة البيانات تدعم `utf8mb4_unicode_ci`

### المشكلة: "المشروع لا يتصل بقاعدة البيانات"
**الحل:**
- تحقق من `DATABASE_URL` في ملف .env
- تأكد من أن قاعدة البيانات تعمل
- راجع ملف `server/db.ts`

---

## ✅ التوصية

**للتطوير المحلي:**
استخدم **SQLite** (الإعداد الحالي) - لا حاجة لتغيير أي شيء!

**للإنتاج:**
استورد **nesma-barzan-mysql.sql** إلى MySQL وعدّل إعدادات الاتصال.

---

**تاريخ الإنشاء:** 29 ديسمبر 2025  
**الحالة:** ✅ جاهز للاستخدام
