# محتوى قاعدة البيانات - local.db

## ✅ الجداول الموجودة

قاعدة البيانات تحتوي على **5 جداول**:
1. `users` - بيانات المستخدمين
2. `site_content` - محتوى الموقع
3. `site_settings` - إعدادات الموقع
4. `projects` - المشاريع
5. `images` - الصور المرفوعة

---

## 📊 البيانات المحملة

### جدول: site_settings (9 إعدادات)

| المفتاح | القيمة |
|---------|--------|
| `site_logo` | `/uploads/logo.png` |
| `site_name_ar` | نسمة برزان التجارية |
| `site_name_en` | Nesma Barzan Trading |
| `foundation_year` | 2005 |
| `contact_phone` | +966 555 499 991 |
| `contact_email` | info@shheer.com |
| `contact_website` | www.shheer.com |
| `contact_address_ar` | الرياض، المملكة العربية السعودية |
| `contact_address_en` | Riyadh, Saudi Arabia |

### جدول: users
- **العدد:** 0 مستخدم
- **الحالة:** فارغ (سيتم إضافة المستخدمين عند تسجيل الدخول)

### جدول: projects
- **العدد:** 0 مشروع
- **الحالة:** فارغ (جاهز لإضافة المشاريع من لوحة التحكم)

### جدول: images
- **العدد:** 0 صورة
- **الحالة:** فارغ (جاهز لرفع الصور من لوحة التحكم)

---

## 📁 معلومات الملف

- **اسم الملف:** `local.db`
- **الحجم:** 44 KB
- **النوع:** SQLite 3 Database
- **الموقع:** `/home/ubuntu/NESMA-BARAZAN/local.db`

---

## 🔍 كيفية فحص قاعدة البيانات

### باستخدام SQLite CLI:
```bash
# عرض الجداول
sqlite3 local.db ".tables"

# عرض محتوى جدول معين
sqlite3 local.db "SELECT * FROM site_settings;"

# عرض عدد السجلات
sqlite3 local.db "SELECT COUNT(*) FROM site_settings;"
```

### باستخدام أدوات GUI:
- **DB Browser for SQLite** (مجاني)
- **SQLiteStudio** (مجاني)
- **DBeaver** (مجاني)

---

## ✅ التأكيد

قاعدة البيانات **ليست فارغة**! تحتوي على:
- ✅ 9 إعدادات محملة مسبقاً
- ✅ جميع الجداول منشأة وجاهزة
- ✅ البيانات الأساسية للموقع موجودة

---

## 📝 ملاحظة

الجداول `users`, `projects`, `images` فارغة بشكل طبيعي لأنها ستمتلئ عند:
- إضافة مشاريع من لوحة التحكم
- رفع صور من لوحة التحكم
- تسجيل مستخدمين (إذا تم تفعيل OAuth)

---

**تاريخ الإنشاء:** 29 ديسمبر 2025  
**الحالة:** ✅ جاهزة للاستخدام
