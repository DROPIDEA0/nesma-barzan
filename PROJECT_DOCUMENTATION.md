# 📋 توثيق مشروع نسمة برزان التجارية - NESMA BARAZAN

> **⚠️ تعليمات صارمة للوكلاء (AI Agents):**
> 
> 1. **هذا هو الملف الوحيد للتوثيق** - يُمنع إنشاء أي ملفات توثيق إضافية (MD files)
> 2. **التحديثات تتم هنا فقط** - أي تحديث أو تعديل يجب أن يكون في هذا الملف
> 3. **لا تنشئ ملفات جديدة** - مثل CHANGELOG.md, DEPLOYMENT.md, GUIDE.md, إلخ
> 4. **استخدم الأقسام الموجودة** - إذا احتجت إضافة معلومات، أضفها في القسم المناسب
> 5. **احتفظ بالبنية** - لا تغير هيكل الملف، فقط حدّث المحتوى
> 6. **التاريخ والإصدار** - حدّث تاريخ آخر تعديل في نهاية الملف عند كل تغيير

---

## 📑 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المميزات](#المميزات)
3. [التقنيات المستخدمة](#التقنيات-المستخدمة)
4. [البنية والهيكل](#البنية-والهيكل)
5. [التثبيت والإعداد](#التثبيت-والإعداد)
6. [قاعدة البيانات](#قاعدة-البيانات)
7. [النشر على Hostinger](#النشر-على-hostinger)
8. [نظام المصادقة](#نظام-المصادقة)
9. [إدارة المحتوى](#إدارة-المحتوى)
10. [حل المشاكل](#حل-المشاكل)
11. [سجل التغييرات](#سجل-التغييرات)

---

## 🌟 نظرة عامة

**نسمة برزان التجارية** هو موقع إلكتروني احترافي مع لوحة تحكم كاملة لإدارة المحتوى، مبني بتقنيات حديثة ومتطورة.

### معلومات المشروع

| المعلومة | القيمة |
|---------|--------|
| **اسم المشروع** | NESMA-BARAZAN |
| **المستودع** | DROPIDEA0/NESMA-BARAZAN |
| **الرابط المؤقت** | https://mediumturquoise-dotterel-343079.hostingersite.com |
| **الرابط الدائم** | shheer.com (سيتم التفعيل لاحقاً) |
| **الإصدار** | 1.0.0 |
| **الحالة** | ✅ جاهز للإنتاج |

---

## 🎯 المميزات

### الموقع الرئيسي
- ✅ تصميم احترافي وعصري متجاوب لجميع الأجهزة
- ✅ دعم كامل للغتين العربية والإنجليزية
- ✅ صفحة رئيسية ديناميكية
- ✅ عرض المشاريع والأعمال
- ✅ معرض الصور التفاعلي
- ✅ نموذج التواصل

### لوحة التحكم (Admin Panel)
- ✅ نظام تسجيل دخول آمن بتشفير bcrypt
- ✅ إدارة المحتوى النصي (Content Management)
- ✅ إدارة المشاريع (Projects Management)
- ✅ إدارة الصور (Images Management)
- ✅ إدارة الإعدادات (Settings)
- ✅ تغيير كلمة المرور
- ✅ واجهة سهلة الاستخدام

---

## 🛠️ التقنيات المستخدمة

### Frontend Stack
| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **React** | 19.2.1 | مكتبة بناء واجهات المستخدم |
| **TypeScript** | 5.9.3 | لغة البرمجة الأساسية |
| **Vite** | 7.1.7 | أداة البناء والتطوير |
| **Tailwind CSS** | 4.1.14 | إطار عمل CSS |
| **Wouter** | 3.3.5 | التوجيه (Routing) |
| **tRPC** | 11.6.0 | التواصل Type-safe مع API |
| **Sonner** | 2.0.7 | نظام الإشعارات |

### Backend Stack
| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **Node.js** | 22.x | بيئة التشغيل |
| **Express** | 4.21.2 | إطار عمل الويب |
| **tRPC Server** | 11.6.0 | API type-safe |
| **Drizzle ORM** | 0.45.1 | التعامل مع قاعدة البيانات |
| **bcryptjs** | 3.0.3 | تشفير كلمات المرور |
| **MySQL2** | 3.15.1 | محرك قاعدة البيانات |

### قاعدة البيانات
- **SQLite** - للتطوير المحلي (Development)
- **MySQL** - للإنتاج على Hostinger (Production)

---

## 📁 البنية والهيكل

```
NESMA-BARAZAN/
├── client/                          # Frontend Application
│   ├── src/
│   │   ├── pages/                   # صفحات التطبيق
│   │   │   ├── Home.tsx            # الصفحة الرئيسية
│   │   │   ├── Login.tsx           # صفحة تسجيل الدخول
│   │   │   └── admin/              # صفحات لوحة التحكم
│   │   │       ├── Dashboard.tsx   # لوحة المعلومات
│   │   │       ├── Content.tsx     # إدارة المحتوى
│   │   │       ├── Projects.tsx    # إدارة المشاريع
│   │   │       ├── Images.tsx      # إدارة الصور
│   │   │       └── ChangePassword.tsx  # تغيير كلمة المرور
│   │   ├── components/             # المكونات القابلة لإعادة الاستخدام
│   │   └── lib/                    # المكتبات المساعدة
│
├── server/                          # Backend Application
│   ├── _core/
│   │   └── index.ts                # نقطة البداية للسيرفر
│   ├── auth.ts                     # نظام المصادقة والجلسات
│   ├── routers.ts                  # تعريف API Routes
│   └── db.ts                       # إعداد قاعدة البيانات
│
├── shared/                          # الملفات المشتركة بين Frontend و Backend
│   └── schema.ts                   # تعريف الـ Schema المشترك
│
├── drizzle/                         # Database Migrations
│   ├── schema.ts                   # تعريف جداول قاعدة البيانات
│   ├── 0000_classy_boom_boom.sql
│   ├── 0001_cool_wendigo.sql
│   └── 0002_wandering_banshee.sql
│
├── migrations/                      # SQL Migrations يدوية
│   └── add_username_password.sql
│
├── DATABASE.sql                     # ملف قاعدة البيانات الموحد (انظر القسم التالي)
├── PROJECT_DOCUMENTATION.md         # هذا الملف - التوثيق الموحد
├── package.json                     # تعريف المشروع والـ Dependencies
├── tsconfig.json                    # إعدادات TypeScript
├── vite.config.ts                   # إعدادات Vite
└── drizzle.config.ts               # إعدادات Drizzle ORM
```

---

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية
- **Node.js** 22.x أو أحدث
- **pnpm** 10.4.1 أو أحدث (مدير الحزم)
- **Git** للاستنساخ من GitHub

### خطوات التثبيت المحلي

```bash
# 1. استنساخ المشروع من GitHub
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# 2. تثبيت جميع Dependencies
pnpm install

# 3. إنشاء ملف .env للتطوير المحلي
cat > .env << EOF
NODE_ENV=development
SESSION_SECRET=your-local-secret-key
EOF

# 4. تشغيل في وضع التطوير
pnpm run dev

# 5. فتح المتصفح
# الموقع الرئيسي: http://localhost:3000
# صفحة تسجيل الدخول: http://localhost:3000/login
# لوحة التحكم: http://localhost:3000/admin
```

### البناء للإنتاج

```bash
# بناء المشروع
pnpm run build

# تشغيل النسخة المبنية
pnpm start
```

### الأوامر المتاحة

| الأمر | الوصف |
|------|-------|
| `pnpm run dev` | تشغيل وضع التطوير مع Hot Reload |
| `pnpm run build` | بناء المشروع للإنتاج |
| `pnpm start` | تشغيل النسخة المبنية |
| `pnpm run check` | فحص أخطاء TypeScript |
| `pnpm run format` | تنسيق الكود باستخدام Prettier |
| `pnpm run test` | تشغيل الاختبارات |
| `pnpm run db:push` | تطبيق تغييرات قاعدة البيانات |

---

## 🗄️ قاعدة البيانات

> **⚠️ تعليمات صارمة للوكلاء:**
> 
> - **ملف واحد فقط للبيانات**: `DATABASE.sql` هو الملف الوحيد لقاعدة البيانات
> - **لا تنشئ ملفات SQL جديدة** في الجذر أو أي مكان آخر
> - **Migrations في مجلد drizzle فقط**: استخدم Drizzle Kit لإنشاء migrations
> - **التحديثات تتم عبر Drizzle**: استخدم `pnpm run db:push` لتطبيق التغييرات

### معلومات قاعدة البيانات على Hostinger

| المعلومة | القيمة |
|---------|--------|
| **اسم قاعدة البيانات** | u521934522_nasma_db |
| **اسم المستخدم** | u521934522_nasma_db_new |
| **كلمة المرور** | uRo2hz3yf0\| |
| **المضيف** | localhost |
| **المنفذ** | 3306 |

### DATABASE_URL للإنتاج

```
mysql://u521934522_nasma_db_new:uRo2hz3yf0|@localhost:3306/u521934522_nasma_db
```

### الجداول الرئيسية

#### 1. جدول `users` - المستخدمين
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**البيانات الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123` (مشفرة بـ bcrypt)

#### 2. جدول `content` - المحتوى
```sql
CREATE TABLE content (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT UNIQUE NOT NULL,
  value TEXT NOT NULL,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. جدول `projects` - المشاريع
```sql
CREATE TABLE projects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT,
  image_url TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 4. جدول `images` - الصور
```sql
CREATE TABLE images (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  url TEXT NOT NULL,
  alt TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### استيراد قاعدة البيانات

**على Hostinger:**
1. اذهب إلى **Databases** في hPanel
2. افتح **phpMyAdmin**
3. اختر قاعدة البيانات `u521934522_nasma_db`
4. اضغط على **Import**
5. ارفع ملف `DATABASE.sql`
6. اضغط **Go**

---

## 🌐 النشر على Hostinger

### خدمة Node.js Web Apps الجديدة

Hostinger تقدم الآن خدمة **Node.js Web Apps Hosting** التي تدعم:
- ربط مباشر مع GitHub
- Auto Deploy تلقائي عند كل Push
- Build تلقائي للمشروع
- إدارة Environment Variables

### الخطط المدعومة
- Business Web Hosting ✅
- Cloud Startup ✅
- Cloud Professional ✅
- Cloud Enterprise ✅

### خطوات النشر

#### 1. إعداد المشروع على GitHub
```bash
# تأكد من رفع آخر التحديثات
git add .
git commit -m "Update project"
git push origin main
```

#### 2. إضافة الموقع في Hostinger
1. سجل الدخول إلى **hPanel**
2. اذهب إلى **Websites** > **Add Website**
3. اختر **Node.js Apps**
4. اختر **Import Git Repository**
5. اضغط **Authorize** للسماح لـ Hostinger بالوصول إلى GitHub
6. اختر المستودع: **DROPIDEA0/NESMA-BARAZAN**
7. اختر الفرع: **main**

#### 3. ضبط Build Settings
سيتم اكتشاف الإعدادات تلقائياً من `package.json`:

| الإعداد | القيمة |
|--------|--------|
| **Build Command** | `pnpm run build` |
| **Start Command** | `pnpm start` |
| **Node Version** | 22.x |
| **Install Command** | `pnpm install` |

#### 4. إضافة Environment Variables

اذهب إلى **Settings and redeploy** > **Environment Variables** وأضف:

```env
DATABASE_URL=mysql://u521934522_nasma_db_new:uRo2hz3yf0|@localhost:3306/u521934522_nasma_db
SESSION_SECRET=nesma-barzan-production-secret-2025-change-this
NODE_ENV=production
PORT=3000
```

⚠️ **مهم:** غيّر `SESSION_SECRET` إلى قيمة عشوائية قوية!

#### 5. Deploy الموقع
1. اضغط **Deploy**
2. انتظر حتى ينتهي البناء (Build)
3. سيتم إنشاء رابط مؤقت تلقائياً
4. تحقق من **Deployment Details** للتأكد من نجاح العملية

### Auto Deploy التلقائي

بعد الربط مع GitHub:
- **كل Push** إلى الفرع `main` سيؤدي إلى **Auto Deploy** تلقائي
- يمكنك رؤية **Build Logs** في لوحة التحكم
- في حالة فشل البناء، ستظهر رسالة الخطأ في **Deployment Details**

### ربط النطاق الدائم

عندما تكون جاهزاً لربط `shheer.com`:

1. اذهب إلى **Websites** > اختر الموقع
2. اضغط **Connect domain**
3. اختر `shheer.com`
4. اتبع التعليمات لتحديث DNS إذا لزم الأمر

---

## 🔐 نظام المصادقة

### بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: admin123
```

⚠️ **مهم جداً:** يجب تغيير كلمة المرور فوراً بعد أول تسجيل دخول!

### كيفية تسجيل الدخول

1. افتح الموقع: https://mediumturquoise-dotterel-343079.hostingersite.com
2. اذهب إلى: `/login`
3. أدخل بيانات الدخول الافتراضية
4. بعد الدخول، اذهب إلى **تغيير كلمة المرور** من القائمة الجانبية

### تغيير كلمة المرور

1. من لوحة التحكم، اضغط على **تغيير كلمة المرور**
2. أدخل كلمة المرور الحالية
3. أدخل كلمة المرور الجديدة
4. أكد كلمة المرور الجديدة
5. اضغط **حفظ**

### الأمان

- **تشفير bcrypt**: كلمات المرور مشفرة بـ 10 salt rounds
- **Session-based**: استخدام الجلسات للمصادقة
- **حماية Routes**: جميع صفحات Admin محمية
- **Middleware**: التحقق من الصلاحيات قبل كل عملية

---

## 📝 إدارة المحتوى

### إدارة المحتوى النصي (Content)

يمكنك تعديل النصوص في الموقع من خلال:
1. **لوحة التحكم** > **إدارة المحتوى**
2. اختر العنصر المراد تعديله
3. عدّل النص
4. اضغط **حفظ**

### إدارة المشاريع (Projects)

لإضافة مشروع جديد:
1. **لوحة التحكم** > **إدارة المشاريع**
2. اضغط **إضافة مشروع**
3. أدخل العنوان والوصف
4. ارفع صورة المشروع
5. اضغط **حفظ**

### إدارة الصور (Images)

لإضافة صورة جديدة:
1. **لوحة التحكم** > **إدارة الصور**
2. اضغط **رفع صورة**
3. اختر الصورة من جهازك
4. أدخل النص البديل (Alt Text)
5. اضغط **رفع**

---

## 🐛 حل المشاكل

### المشكلة: الموقع لا يعمل بعد النشر

**الحلول:**
1. تحقق من **Build Logs** في Hostinger
2. تأكد من إضافة جميع **Environment Variables**
3. تحقق من صحة `DATABASE_URL`
4. تأكد من أن قاعدة البيانات تحتوي على البيانات

### المشكلة: لا يمكن تسجيل الدخول

**الحلول:**
1. تأكد من استيراد ملف `DATABASE.sql` إلى قاعدة البيانات
2. تحقق من أن جدول `users` يحتوي على المستخدم `admin`
3. استخدم البيانات الافتراضية: `admin` / `admin123`
4. امسح الـ Cookies وحاول مرة أخرى

### المشكلة: Build يفشل على Hostinger

**الحلول:**
1. تحقق من أن `package.json` يحتوي على جميع الـ Dependencies
2. تأكد من أن `Node.js version` متوافق (22.x)
3. راجع **Build Logs** لمعرفة الخطأ المحدد
4. تأكد من أن `pnpm-lock.yaml` محدث

### المشكلة: قاعدة البيانات لا تتصل

**الحلول:**
1. تحقق من صحة `DATABASE_URL` في Environment Variables
2. تأكد من أن قاعدة البيانات موجودة على Hostinger
3. تحقق من بيانات الاتصال (اسم المستخدم، كلمة المرور)
4. تأكد من أن المستخدم لديه صلاحيات على قاعدة البيانات

---

## 📋 سجل التغييرات

### الإصدار 1.0.0 - 30 ديسمبر 2025

#### ✨ إضافات جديدة
- ✅ توحيد جميع ملفات التوثيق في `PROJECT_DOCUMENTATION.md`
- ✅ توحيد جميع ملفات SQL في `DATABASE.sql`
- ✅ إضافة تعليمات صارمة للوكلاء (AI Agents)
- ✅ توثيق آلية Auto Deploy من Hostinger
- ✅ إضافة معلومات قاعدة البيانات الكاملة

#### 🔧 تحسينات
- ✅ تنظيف المشروع من الملفات الزائدة
- ✅ تحسين بنية المشروع
- ✅ تحديث معلومات النشر

#### 📝 توثيق
- ✅ دليل شامل للتثبيت والإعداد
- ✅ دليل النشر على Hostinger
- ✅ دليل حل المشاكل الشائعة

### الإصدار 0.9.0 - 29 ديسمبر 2025

#### ✨ إضافات سابقة
- ✅ نظام تسجيل الدخول الآمن
- ✅ تشفير كلمات المرور بـ bcrypt
- ✅ صفحة تغيير كلمة المرور
- ✅ حماية جميع صفحات Admin
- ✅ Session-based authentication
- ✅ إدارة المحتوى والمشاريع والصور

---

## 📞 الدعم والمساعدة

للحصول على المساعدة:
1. راجع قسم **حل المشاكل** أعلاه
2. تحقق من **Build Logs** في Hostinger
3. افتح **Issue** على GitHub إذا لزم الأمر

---

## 📝 الترخيص

هذا المشروع ملك لشركة **نسمة برزان التجارية**.  
جميع الحقوق محفوظة © 2025

---

## 🎯 الخلاصة

هذا المشروع جاهز للإنتاج ويعمل بكفاءة على Hostinger مع:
- ✅ Auto Deploy تلقائي من GitHub
- ✅ قاعدة بيانات MySQL محدثة
- ✅ نظام مصادقة آمن
- ✅ لوحة تحكم كاملة
- ✅ توثيق شامل

---

**آخر تحديث:** 30 ديسمبر 2025  
**الإصدار:** 1.0.0  
**الحالة:** ✅ جاهز للإنتاج  
**المطور:** Manus AI Agent
