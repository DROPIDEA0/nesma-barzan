# 📋 توثيق مشروع نسمة برزان التجارية - NESMA BARAZAN

---

## ⚠️ قواعد صارمة للوكلاء (AI Agents Strict Rules)

هذا القسم إلزامي لجميع وكلاء الذكاء الاصطناعي العاملين على هذا المشروع. يجب قراءته والالتزام به قبل إجراء أي تعديل.

### الملفات المسموحة

| نوع الملف | الملف المسموح | الغرض |
|-----------|---------------|-------|
| التوثيق | `PROJECT_DOCUMENTATION.md` | الملف الوحيد للتوثيق الشامل |
| التعريف | `README.md` | ملف تعريفي مختصر |
| قاعدة البيانات | `DATABASE.sql` | ملف قاعدة البيانات الموحد في الجذر |
| Migrations | `drizzle/*.sql` | ملفات Drizzle المُنشأة تلقائياً |

### الملفات الممنوعة

يُمنع منعاً باتاً إنشاء أي ملفات MD إضافية مثل CHANGELOG.md أو DEPLOYMENT.md أو TODO.md أو أي ملف توثيق آخر. كذلك يُمنع إنشاء ملفات SQL جديدة في جذر المشروع غير DATABASE.sql.

### قواعد التحديث

عند الحاجة لإضافة معلومات جديدة، يجب تحديث هذا الملف في القسم المناسب وتحديث تاريخ آخر تعديل في نهاية الملف. لا يجوز إنشاء ملفات جديدة للتوثيق تحت أي ظرف.

### قواعد قاعدة البيانات

لتعديل قاعدة البيانات، يجب استخدام Drizzle ORM عبر الأمر `pnpm run db:push`. للاستيراد اليدوي، يُستخدم ملف DATABASE.sql فقط. ملفات Migrations تُحفظ تلقائياً في مجلد drizzle/.

---

## 📑 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المميزات](#المميزات)
3. [التقنيات المستخدمة](#التقنيات-المستخدمة)
4. [البنية والهيكل](#البنية-والهيكل)
5. [التثبيت والإعداد](#التثبيت-والإعداد)
6. [قاعدة البيانات](#قاعدة-البيانات)
7. [النشر على Hostinger](#النشر-على-hostinger)
8. [نظام المصادقة](#نظام-المصادقة)
9. [إدارة المحتوى](#إدارة-المحتوى)
10. [حل المشاكل](#حل-المشاكل)
11. [سجل التغييرات](#سجل-التغييرات)

---

## 🌟 نظرة عامة

**نسمة برزان التجارية** هو موقع إلكتروني احترافي مع لوحة تحكم كاملة لإدارة المحتوى، مبني بتقنيات حديثة ومتطورة.

### معلومات المشروع

| المعلومة | القيمة |
|---------|--------|
| **اسم المشروع** | NESMA-BARAZAN |
| **المستودع** | DROPIDEA0/NESMA-BARAZAN |
| **الرابط المؤقت** | https://mediumturquoise-dotterel-343079.hostingersite.com |
| **الرابط الدائم** | shheer.com (سيتم التفعيل لاحقاً) |
| **الإصدار** | 1.0.1 |
| **الحالة** | ✅ جاهز للإنتاج |

---

## 🎯 المميزات

### الموقع الرئيسي

الموقع يتميز بتصميم احترافي وعصري متجاوب لجميع الأجهزة مع دعم كامل للغتين العربية والإنجليزية. يحتوي على صفحة رئيسية ديناميكية وعرض للمشاريع والأعمال ومعرض صور تفاعلي ونموذج للتواصل.

### لوحة التحكم (Admin Panel)

لوحة التحكم توفر نظام تسجيل دخول آمن بتشفير bcrypt مع إمكانية إدارة المحتوى النصي والمشاريع والصور والإعدادات. كما تتيح تغيير كلمة المرور وتوفر واجهة سهلة الاستخدام.

---

## 🛠️ التقنيات المستخدمة

### Frontend Stack

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **React** | 19.2.1 | مكتبة بناء واجهات المستخدم |
| **TypeScript** | 5.9.3 | لغة البرمجة الأساسية |
| **Vite** | 7.1.7 | أداة البناء والتطوير |
| **Tailwind CSS** | 4.1.14 | إطار عمل CSS |
| **Wouter** | 3.3.5 | التوجيه (Routing) |
| **tRPC** | 11.6.0 | التواصل Type-safe مع API |

### Backend Stack

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **Node.js** | 22.x | بيئة التشغيل |
| **Express** | 4.21.2 | إطار عمل الويب |
| **tRPC Server** | 11.6.0 | API type-safe |
| **Drizzle ORM** | 0.45.1 | التعامل مع قاعدة البيانات |
| **bcryptjs** | 3.0.3 | تشفير كلمات المرور |
| **MySQL2** | 3.15.1 | محرك قاعدة البيانات |

---

## 📁 البنية والهيكل

```
NESMA-BARAZAN/
├── client/                          # Frontend Application
│   ├── src/
│   │   ├── pages/                   # صفحات التطبيق
│   │   │   ├── Home.tsx            # الصفحة الرئيسية
│   │   │   ├── Login.tsx           # صفحة تسجيل الدخول
│   │   │   └── admin/              # صفحات لوحة التحكم
│   │   ├── components/             # المكونات القابلة لإعادة الاستخدام
│   │   └── lib/                    # المكتبات المساعدة
│
├── server/                          # Backend Application
│   ├── _core/
│   │   ├── index.ts                # نقطة البداية للسيرفر
│   │   └── env.ts                  # متغيرات البيئة
│   ├── auth.ts                     # نظام المصادقة والجلسات
│   ├── routers.ts                  # تعريف API Routes
│   ├── logger.ts                   # نظام التسجيل
│   └── db.ts                       # إعداد قاعدة البيانات
│
├── shared/                          # الملفات المشتركة
├── drizzle/                         # Database Schema & Migrations
├── DATABASE.sql                     # ملف قاعدة البيانات الموحد
├── PROJECT_DOCUMENTATION.md         # هذا الملف - التوثيق الشامل
└── README.md                        # ملف تعريفي مختصر
```

---

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية

المشروع يتطلب Node.js إصدار 22.x أو أحدث، ومدير الحزم pnpm إصدار 10.4.1 أو أحدث، وGit للاستنساخ من GitHub.

### خطوات التثبيت المحلي

```bash
# 1. استنساخ المشروع من GitHub
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# 2. تثبيت جميع Dependencies
pnpm install

# 3. تشغيل في وضع التطوير
pnpm run dev
```

### الأوامر المتاحة

| الأمر | الوصف |
|------|-------|
| `pnpm run dev` | تشغيل وضع التطوير مع Hot Reload |
| `pnpm run build` | بناء المشروع للإنتاج |
| `pnpm start` | تشغيل النسخة المبنية |
| `pnpm run db:push` | تطبيق تغييرات قاعدة البيانات |

---

## 🗄️ قاعدة البيانات

### معلومات قاعدة البيانات على Hostinger

| المعلومة | القيمة |
|---------|--------|
| **اسم قاعدة البيانات** | u521934522_nasma_db |
| **اسم المستخدم** | u521934522_nasma_db_new |
| **كلمة المرور** | Downy144168@144168 |
| **المضيف** | localhost |
| **المنفذ** | 3306 |

### DATABASE_URL للإنتاج

```
mysql://u521934522_nasma_db_new:Downy144168@144168@localhost:3306/u521934522_nasma_db
```

### الجداول الرئيسية

قاعدة البيانات تحتوي على جداول users للمستخدمين، وsite_content للمحتوى، وsite_settings للإعدادات، وprojects للمشاريع، وimages للصور، وfeatures للمميزات، وhero_stats للإحصائيات، وnavigation_items لعناصر التنقل.

---

## 🌐 النشر على Hostinger

### خدمة Node.js Web Apps الجديدة

Hostinger تقدم خدمة Node.js Web Apps Hosting التي تدعم الربط المباشر مع GitHub والـ Auto Deploy التلقائي عند كل Push والبناء التلقائي للمشروع وإدارة Environment Variables.

### الخطط المدعومة

الخدمة متاحة على Business Web Hosting وCloud Startup وCloud Professional وCloud Enterprise وCloud Enterprise Plus.

### خطوات النشر

1. تسجيل الدخول إلى hPanel
2. الذهاب إلى Websites ثم Add Website
3. اختيار Node.js Apps
4. اختيار Import Git Repository
5. تفويض GitHub للوصول
6. اختيار المستودع DROPIDEA0/NESMA-BARAZAN
7. ضبط Build Settings (يتم اكتشافها تلقائياً)
8. إضافة Environment Variables
9. الضغط على Deploy

### Environment Variables المطلوبة

```env
DATABASE_URL=mysql://u521934522_nasma_db_new:Downy144168@144168@localhost:3306/u521934522_nasma_db
DB_HOST=localhost
DB_PORT=3306
DB_USER=u521934522_nasma_db_new
DB_PASSWORD=Downy144168@144168
DB_NAME=u521934522_nasma_db
SESSION_SECRET=nesma-barzan-production-secret-2025
NODE_ENV=production
PORT=3000
```

### Auto Deploy التلقائي

بعد الربط مع GitHub، كل Push إلى الفرع main يؤدي إلى Auto Deploy تلقائي. يمكن رؤية Build Logs في لوحة التحكم، وفي حالة فشل البناء، ستظهر رسالة الخطأ في Deployment Details.

---

## 🔐 نظام المصادقة

### بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: admin123
```

**تحذير:** يجب تغيير كلمة المرور فوراً بعد أول تسجيل دخول.

### الأمان

النظام يستخدم تشفير bcrypt بـ 10 salt rounds لكلمات المرور، ونظام جلسات Session-based للمصادقة، وحماية لجميع صفحات Admin، وMiddleware للتحقق من الصلاحيات.

---

## 📝 إدارة المحتوى

### إدارة المحتوى النصي

يمكن تعديل النصوص في الموقع من خلال لوحة التحكم ثم إدارة المحتوى، واختيار العنصر المراد تعديله، وتعديل النص، ثم الضغط على حفظ.

### إدارة المشاريع

لإضافة مشروع جديد، يتم الذهاب إلى لوحة التحكم ثم إدارة المشاريع، والضغط على إضافة مشروع، وإدخال العنوان والوصف، ورفع صورة المشروع، ثم الضغط على حفظ.

---

## 🐛 حل المشاكل

### مشكلة تسجيل الدخول لا يعمل

**السبب الجذري:** التطبيق في Production يحتاج إلى متغيرات البيئة للاتصال بقاعدة البيانات MySQL.

**الحل:**
1. التأكد من إضافة جميع Environment Variables في Hostinger
2. التأكد من صحة بيانات قاعدة البيانات
3. إعادة النشر بعد إضافة المتغيرات
4. التحقق من أن جدول users يحتوي على المستخدم admin

### مشكلة Build يفشل على Hostinger

**الحلول:**
1. التحقق من أن package.json يحتوي على جميع Dependencies
2. التأكد من أن Node.js version متوافق (22.x)
3. مراجعة Build Logs لمعرفة الخطأ المحدد
4. التأكد من أن pnpm-lock.yaml محدث

### مشكلة قاعدة البيانات لا تتصل

**الحلول:**
1. التحقق من صحة DATABASE_URL في Environment Variables
2. التأكد من أن قاعدة البيانات موجودة على Hostinger
3. التحقق من بيانات الاتصال
4. التأكد من أن المستخدم لديه صلاحيات على قاعدة البيانات

---

## 📋 سجل التغييرات

### الإصدار 1.0.1 - 30 ديسمبر 2025

تم توحيد جميع ملفات التوثيق في هذا الملف الواحد، وحذف الملفات الزائدة مثل FIX_LOGIN_SOLUTION.md و.AI_AGENTS_RULES.md. تم تحديث بيانات قاعدة البيانات بالمعلومات الصحيحة، وإضافة قواعد صارمة للوكلاء في بداية الملف.

### الإصدار 1.0.0 - 30 ديسمبر 2025

تم إنشاء التوثيق الأولي الشامل للمشروع مع جميع المعلومات اللازمة للتثبيت والنشر والإدارة.

---

## 📞 الدعم والمساعدة

للحصول على المساعدة، يُرجى مراجعة قسم حل المشاكل أعلاه، أو التحقق من Build Logs في Hostinger، أو فتح Issue على GitHub إذا لزم الأمر.

---

## 📝 الترخيص

هذا المشروع ملك لشركة **نسمة برزان التجارية**.  
جميع الحقوق محفوظة © 2025

---

**آخر تحديث:** 30 ديسمبر 2025  
**الإصدار:** 1.0.1  
**الحالة:** ✅ جاهز للإنتاج
