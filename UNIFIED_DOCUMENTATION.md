# 📜 التوثيق الشامل والموحد - نسمة برزان التجارية

**آخر تحديث:** 02 يناير 2026
**الإصدار:** 2.2.0
**الحالة:** ✅ جاهز للإنتاج على VPS

---

> ### ⚠️ تعليمات صارمة للفريق والمطورين والوكلاء
>
> 1.  **هذا هو الملف المرجعي الوحيد للتوثيق.** يُمنع منعاً باتاً إنشاء أو استخدام أي ملفات `.md` أخرى. تم دمج جميع ملفات التوثيق السابقة (`README.md`, `DEPLOYMENT.md`, `PROJECT_DOCUMENTATION.md`, `TODO.md`) في هذا الملف وحذفها نهائياً.
> 2.  **جميع التحديثات المستقبلية للتوثيق يجب أن تتم على هذا الملف فقط.** أي محاولة لإنشاء ملفات `.md` إضافية ستُرفض.
> 3.  **قاعدة البيانات لها ملف واحد فقط وهو `DATABASE.sql` في جذر المشروع.** هذا الملف يحتوي على أحدث بنية كاملة من الخادم. تم دمج جميع ملفات SQL الإضافية (`add_contact_features.sql`, `add_new_features.sql`) في `DATABASE.sql` وحذفها نهائياً.
> 4.  **عند كل رفع (Push) جديد إلى GitHub، يجب تحديث هذا الملف بالمعلومات الأحدث فقط.** لا تضف ملفات جديدة، بل حدّث المحتوى الموجود.
> 5.  **الالتزام بهذه القواعد إلزامي لضمان توحيد العمل وسهولة الصيانة.**
> 6.  **تم الانتقال من استضافة Shared Hosting إلى VPS مخصص.** جميع المعلومات والإعدادات في هذا الملف محدثة لتعكس بيئة VPS الجديدة.

---

## 📋 جدول المحتويات

1.  [نظرة عامة على المشروع](#-نظرة-عامة-على-المشروع)
2.  [الروابط المهمة](#-الروابط-المهمة)
3.  [التقنيات المستخدمة](#-التقنيات-المستخدمة)
4.  [البدء السريع](#-البدء-السريع)
5.  [التثبيت والإعداد المحلي](#-التثبيت-والإعداد-المحلي)
6.  [بيئة الإنتاج (VPS)](#-بيئة-الإنتاج-vps)
7.  [قاعدة البيانات (VPS)](#-قاعدة-البيانات-vps)
8.  [النشر والتحديث (Auto-Deploy)](#-النشر-والتحديث-auto-deploy)
9.  [إدارة الخدمة على الخادم (systemd)](#-إدارة-الخدمة-على-الخادم-systemd)
10. [إعدادات خادم الويب (Nginx)](#-إعدادات-خادم-الويب-nginx)
11. [إدارة اللوجوهات والمحتوى المرئي](#-إدارة-اللوجوهات-والمحتوى-المرئي)
12. [نظام المصادقة ولوحة التحكم](#-نظام-المصادقة-ولوحة-التحكم)
13. [قائمة المهام والتحديثات الأخيرة](#-قائمة-المهام-والتحديثات-الأخيرة)
14. [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)
15. [الترخيص](#-الترخيص)

---

## 🏢 نظرة عامة على المشروع

موقع إلكتروني احترافي لشركة **نسمة برزان التجارية**، مع لوحة تحكم كاملة لإدارة المحتوى، تم نقله وتحديثه للعمل على خادم VPS خاص لضمان أفضل أداء وأمان.

### المميزات الرئيسية

*   **الموقع:** تصميم احترافي متجاوب، ثنائي اللغة (عربي/إنجليزي)، عرض للمشاريع، معرض صور، ونموذج تواصل.
*   **لوحة التحكم:** نظام دخول آمن، إدارة كاملة للمحتوى (نصوص، مشاريع، صور، إعدادات)، وتغيير كلمة المرور.
*   **البنية التحتية:** تم الانتقال من استضافة Shared Hosting إلى VPS مخصص على Hostinger لتحسين الأداء والتحكم الكامل.

---

## 🔗 الروابط المهمة

| الوصف | الرابط |
| :--- | :--- |
| **الموقع الرئيسي** | `https://www.shheer.com` |
| **لوحة التحكم** | `https://www.shheer.com/admin` |
| **مستودع GitHub** | `https://github.com/DROPIDEA0/NESMA-BARAZAN` |
| **لوحة إدارة السيرفر** | `https://resq.click/` |

---

## 🛠️ التقنيات المستخدمة

| المجال | التقنية | الإصدار |
| :--- | :--- | :--- |
| **Frontend** | React, TypeScript, Vite, TailwindCSS | `19.2.1`, `5.9.3`, `7.1.7`, `4.1.14` |
| **Backend** | Node.js, Express, tRPC, Drizzle ORM | `22.x`, `4.21.2`, `11.6.0`, `0.45.1` |
| **Database** | MySQL | `8.4.6` |
| **Infrastructure** | Ubuntu, Nginx, systemd | `22.04`, `1.28.0`, `systemd` |

---

## 🚀 البدء السريع

```bash
# استنساخ المشروع
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# تثبيت الاعتماديات
pnpm install

# تشغيل وضع التطوير
pnpm run dev
```

---

## 🚀 التثبيت والإعداد المحلي

### المتطلبات الأساسية

*   **Node.js:** الإصدار 18 أو أحدث
*   **pnpm:** مدير الحزم (يمكن تثبيته عبر `npm install -g pnpm`)
*   **MySQL:** قاعدة بيانات محلية للتطوير

### خطوات التثبيت

```bash
# 1. استنساخ المشروع
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# 2. تثبيت الاعتماديات
pnpm install

# 3. إعداد ملف .env
# انسخ ملف .env.production وعدّل بيانات قاعدة البيانات المحلية
cp .env.production .env

# 4. تشغيل وضع التطوير
pnpm run dev
```

### ملف .env للتطوير المحلي

```env
DATABASE_URL="mysql://username:password@localhost:3306/database_name"
NODE_ENV=development
PORT=3000
```

---

## 🌐 بيئة الإنتاج (VPS)

| المعلومة | القيمة |
| :--- | :--- |
| **نوع الاستضافة** | VPS (Hostinger) |
| **IP Address** | `72.62.7.159` |
| **نظام التشغيل** | Ubuntu 22.04 |
| **مسار المشروع** | `/home/shheercom/htdocs/www.shheer.com` |

### بيانات الدخول للخادم (SSH)

```bash
ssh root@72.62.7.159
# كلمة المرور: Downy144168@#144168
```

### بيانات الدخول للوحة إدارة السيرفر

| المعلومة | القيمة |
| :--- | :--- |
| **الرابط** | `https://resq.click/` |
| **اسم المستخدم** | `admin` |
| **كلمة المرور** | `Downy144168@#5519971930` |

---

## 🗄️ قاعدة البيانات (VPS)

### بيانات الاتصال المحدثة

| المعلومة | القيمة |
| :--- | :--- |
| **Host** | `127.0.0.1` |
| **Port** | `3306` |
| **اسم قاعدة البيانات** | `u521934522-nasma-db` |
| **اسم المستخدم** | `u521934522-nasma-db-new` |
| **كلمة المرور** | `Downy144168@144168` |

### ملف .env على الخادم
يجب أن يحتوي ملف `.env` على الخادم على متغير `DATABASE_URL` التالي:

```env
DATABASE_URL="mysql://u521934522-nasma-db-new:Downy144168@144168@127.0.0.1:3306/u521934522-nasma-db"
NODE_ENV=production
PORT=3000
```

### تحديث قاعدة البيانات

ملف `DATABASE.sql` في جذر المشروع يحتوي على أحدث بنية كاملة لقاعدة البيانات مع جميع الجداول والبيانات. لتحديث قاعدة البيانات على الخادم:

```bash
# الاتصال بالخادم
ssh root@72.62.7.159

# استيراد قاعدة البيانات
mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db < /home/shheercom/htdocs/www.shheer.com/DATABASE.sql
```

### جداول قاعدة البيانات

قاعدة البيانات تحتوي على الجداول التالية:

1. **contact_messages** - رسائل التواصل من الزوار
2. **features** - الميزات والخدمات
3. **hero_stats** - إحصائيات القسم الرئيسي
4. **images** - معرض الصور
5. **navigation_items** - عناصر القائمة
6. **projects** - المشاريع
7. **site_content** - محتوى الموقع
8. **site_settings** - إعدادات الموقع
9. **users** - مستخدمي لوحة التحكم

---

## 🔄 النشر والتحديث (Auto-Deploy)

تم إعداد سكريبت `deploy.sh` على الخادم لأتمتة عملية النشر. لتحديث الموقع، اتبع الخطوات التالية:

### طريقة النشر التلقائي

1.  قم بعمل `push` للتحديثات الجديدة إلى فرع `main` في GitHub.
2.  اتصل بالخادم عبر SSH.
3.  نفذ الأمر التالي:

```bash
# انتقل إلى مسار المشروع وقم بتشغيل سكريبت النشر
cd /home/shheercom/htdocs/www.shheer.com && bash deploy.sh
```

السكريبت سيقوم بسحب التحديثات، تثبيت الاعتماديات، بناء المشروع، وإعادة تشغيل الخدمة تلقائياً.

### محتوى سكريبت deploy.sh

```bash
#!/bin/bash

echo "🚀 بدء عملية النشر التلقائي..."

# سحب آخر التحديثات من GitHub
echo "📥 سحب التحديثات من GitHub..."
git pull origin main

# تثبيت الاعتماديات
echo "📦 تثبيت الاعتماديات..."
pnpm install

# بناء المشروع
echo "🔨 بناء المشروع..."
pnpm run build

# إعادة تشغيل الخدمة
echo "🔄 إعادة تشغيل الخدمة..."
systemctl restart nesma-barzan.service

echo "✅ تم النشر بنجاح!"
```

---

## ⚙️ إدارة الخدمة على الخادم (systemd)

المشروع يعمل كخدمة دائمة باستخدام `systemd`.

*   **ملف الخدمة:** `/etc/systemd/system/nesma-barzan.service`
*   **أوامر التحكم:**

```bash
# عرض حالة الخدمة
systemctl status nesma-barzan.service

# إعادة تشغيل الخدمة (بعد التحديث)
systemctl restart nesma-barzan.service

# إيقاف الخدمة
systemctl stop nesma-barzan.service

# تشغيل الخدمة
systemctl start nesma-barzan.service

# عرض السجلات (Logs)
journalctl -u nesma-barzan.service -f --no-pager
```

### محتوى ملف nesma-barzan.service

```ini
[Unit]
Description=Nesma Barzan Trading Website
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/home/shheercom/htdocs/www.shheer.com
ExecStart=/usr/bin/node /home/shheercom/htdocs/www.shheer.com/dist/index.js
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

---

## 🌐 إعدادات خادم الويب (Nginx)

Nginx يعمل كـ Reverse Proxy لتوجيه الطلبات من المنفذ 80/443 إلى خدمة Node.js على المنفذ 3000.

*   **ملف الإعداد:** `/etc/nginx/sites-enabled/www.shheer.com.conf`
*   **أوامر التحكم:**

```bash
# اختبار صحة الإعدادات
nginx -t

# إعادة تحميل Nginx
systemctl reload nginx

# إعادة تشغيل Nginx
systemctl restart nginx
```

### محتوى ملف www.shheer.com.conf

```nginx
server {
    listen 80;
    server_name www.shheer.com shheer.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 🎨 إدارة اللوجوهات والمحتوى المرئي

### نظام اللوجوهات الثلاثة

تم تنظيم إدارة اللوجوهات في الموقع بحيث يكون هناك ثلاثة لوجوهات مستقلة:

#### 1. لوجو Header/Footer + Favicon
- **الموقع:** صفحة الإعدادات (`/admin/settings`)
- **الوصف:** اللوجو الذي يظهر في رأس وتذييل الموقع، بالإضافة إلى أيقونة الموقع (Favicon)
- **طريقة الرفع:** رفع صورة مباشر من صفحة الإعدادات
- **الحقول في قاعدة البيانات:** `site_settings.site_logo` و `site_settings.site_favicon`

#### 2. اللوجو الثاني (Hero Section)
- **الموقع:** صفحة إدارة المحتوى (`/admin/content`) → تبويب "القسم الرئيسي"
- **الوصف:** اللوجو الكبير الذي يظهر في القسم الرئيسي (Hero) من الصفحة الرئيسية
- **طريقة الرفع:** رفع صورة مباشر مع معاينة فورية
- **الحقل في قاعدة البيانات:** `site_content.hero_logo`
- **التسمية:** "اللوجو الثاني" (عربي) / "Second Logo" (إنجليزي)

#### 3. لوجو البري لود (Preloader)
- **الموقع:** صفحة إدارة المحتوى (`/admin/content`) → تبويب "البري لود"
- **الوصف:** اللوجو والإعدادات المرئية لشاشة التحميل
- **المكونات:**
  - **لوجو البري لود:** صورة تظهر أثناء تحميل الموقع (`site_content.preloader_logo`)
  - **لون دائرة التحميل:** لون الدائرة المتحركة مع Color Picker (`site_content.preloader_color`)
- **طريقة الرفع:** رفع صورة مباشر + اختيار لون من Color Picker

### ملاحظات مهمة

- جميع اللوجوهات تدعم رفع الصور بصيغ: PNG, JPG, JPEG, SVG, WebP
- يتم حفظ الصور في مجلد `/uploads` على الخادم
- التغييرات تظهر فوراً بعد الحفظ وتحديث الصفحة
- تم حذف حقل `header_logo` من إدارة المحتوى لتجنب التكرار

---

## 🔐 نظام المصادقة ولوحة التحكم

### بيانات الدخول الافتراضية

| الرابط | اسم المستخدم | كلمة المرور |
| :--- | :--- | :--- |
| `https://www.shheer.com/admin` | `admin` | `admin123` |

**⚠️ تنبيه هام:** يجب تغيير كلمة المرور فوراً بعد أول تسجيل دخول من داخل لوحة التحكم.

### تغيير كلمة المرور

1.  سجل الدخول إلى لوحة التحكم
2.  انتقل إلى صفحة الإعدادات
3.  استخدم نموذج تغيير كلمة المرور
4.  أدخل كلمة المرور الحالية والجديدة
5.  احفظ التغييرات

---

## 📋 قائمة المهام والتحديثات الأخيرة

### التحديثات المكتملة (01-02 يناير 2026)

#### ✅ تحديث صفحة التواصل
- [x] إضافة خلفية سوداء مزرقة مثل الصفحة الرئيسية
- [x] إضافة Header (المنيو) الموحد
- [x] إضافة Footer الموحد
- [x] إضافة إطار ذهبي رفيع للبوكسات
- [x] إضافة تأثير hover (إطار أعرض + gradient أزرق متحرك)
- [x] ترتيب البوكسات بشكل احترافي
- [x] تحسين تخطيط الخريطة
- [x] تحسين تصميم الفورم

#### ✅ إلغاء قسم التواصل من الصفحة الرئيسية
- [x] البحث عن قسم التواصل في Home.tsx
- [x] حذف/تعليق قسم التواصل
- [x] التأكد من عدم وجود أخطاء

#### ✅ إضافة روابط التواصل
- [x] إضافة رابط "تواصل معنا" في المنيو (Header)
- [x] إضافة رابط "تواصل معنا" في الفوتر
- [x] التأكد من عمل الروابط

#### ✅ إصلاح إرسال الفورم
- [x] ربط الفورم بـ API endpoint
- [x] إضافة رسالة نجاح عند الإرسال
- [x] إضافة رسالة خطأ عند الفشل
- [x] مسح الفورم بعد الإرسال الناجح
- [x] حفظ الرسائل في قاعدة البيانات

#### ✅ الانتقال إلى VPS
- [x] نقل المشروع من Shared Hosting إلى VPS
- [x] إعداد بيئة Node.js على VPS
- [x] إعداد قاعدة البيانات MySQL
- [x] إعداد Nginx كـ Reverse Proxy
- [x] إعداد خدمة systemd للتشغيل الدائم
- [x] إعداد سكريبت Auto-Deploy

### المهام القادمة

- [ ] إضافة شهادة SSL (HTTPS)
- [ ] تحسين أداء الموقع
- [ ] إضافة نظام النسخ الاحتياطي التلقائي
- [ ] تحسين SEO
- [ ] إضافة Google Analytics

---

## 🐛 حل المشاكل الشائعة

### الموقع لا يعمل (502 Bad Gateway)

1.  تأكد من أن خدمة `nesma-barzan.service` تعمل:
    ```bash
    systemctl status nesma-barzan.service
    ```
2.  إذا كانت لا تعمل، افحص السجلات:
    ```bash
    journalctl -u nesma-barzan.service -n 50 --no-pager
    ```
3.  أعد تشغيل الخدمة:
    ```bash
    systemctl restart nesma-barzan.service
    ```

### التحديثات لا تظهر

1.  تأكد من أنك قمت بتشغيل `deploy.sh` بعد الـ `push`
2.  امسح الكاش في متصفحك (Ctrl + Shift + R)
3.  تحقق من أن البناء تم بنجاح في السجلات

### مشاكل قاعدة البيانات

1.  تحقق من صحة بيانات الاتصال في ملف `.env`
2.  تأكد من أن خدمة MySQL تعمل:
    ```bash
    systemctl status mysql
    ```
3.  اختبر الاتصال بقاعدة البيانات:
    ```bash
    mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db
    ```

### مشاكل الصلاحيات

1.  تأكد من صلاحيات مجلد المشروع:
    ```bash
    chown -R root:root /home/shheercom/htdocs/www.shheer.com
    chmod -R 755 /home/shheercom/htdocs/www.shheer.com
    ```
2.  تأكد من صلاحيات مجلد uploads:
    ```bash
    chmod -R 777 /home/shheercom/htdocs/www.shheer.com/uploads
    ```

---

## 📝 الترخيص

هذا المشروع ملك لشركة **نسمة برزان التجارية**. جميع الحقوق محفوظة © 2025-2026.

---

## 📞 الدعم والتواصل

للحصول على الدعم الفني أو الإبلاغ عن مشاكل:
- **الموقع الرسمي:** https://www.shheer.com
- **مستودع GitHub:** https://github.com/DROPIDEA0/NESMA-BARAZAN

---

**آخر تحديث:** 02 يناير 2026 | **الإصدار:** 2.2.0
