# 📜 التوثيق الشامل والموحد - نسمة برزان التجارية

**آخر تحديث:** 04 يناير 2026  
**الإصدار:** 4.0.0  
**الحالة:** ✅ جاهز للإنتاج على VPS

---

## ⚠️ تعليمات صارمة وإلزامية للفريق والمطورين والوكلاء

> ### 🚨 قواعد إلزامية - يُمنع مخالفتها
>
> #### 1. **هذا هو الملف المرجعي الوحيد للتوثيق**
> - ✅ **الملف الوحيد المسموح:** `DOCUMENTATION.md`
> - ❌ **ممنوع منعاً باتاً:** إنشاء أو استخدام أي ملفات `.md` أخرى في جذر المشروع
> - ❌ **ممنوع:** إنشاء ملفات مثل `README.md`, `TODO.md`, `DEPLOYMENT.md`, `NOTES.md`, `DEVELOPER_GUIDELINES.md`, `UNIFIED_DOCUMENTATION.md`
> - تم دمج جميع ملفات التوثيق السابقة في هذا الملف وحذفها نهائياً
>
> #### 2. **قاعدة البيانات لها ملف واحد فقط**
> - ✅ **الملف الوحيد المسموح:** `DATABASE.sql` في جذر المشروع
> - ❌ **ممنوع:** رفع ملفات SQL إضافية مثل `schema.sql`, `migration.sql`, `backup.sql`
> - ❌ **ممنوع:** رفع نسخ احتياطية لقاعدة البيانات إلى GitHub
> - هذا الملف يحتوي على أحدث بنية كاملة من الخادم
>
> #### 3. **التحديثات المستقبلية**
> - ✅ جميع التحديثات للتوثيق يجب أن تتم على هذا الملف فقط
> - ✅ عند كل رفع (Push) جديد إلى GitHub، حدّث هذا الملف بالمعلومات الأحدث فقط
> - ❌ لا تضف ملفات جديدة، بل حدّث المحتوى الموجود في هذا الملف
> - ✅ قبل كل Push، تأكد من حذف أي ملفات `.md` أو `.sql` إضافية
>
> #### 4. **بيئة الإنتاج الحالية**
> - ✅ تم الانتقال من استضافة Shared Hosting (Hostinger) إلى **VPS مخصص**
> - ✅ جميع المعلومات والإعدادات في هذا الملف محدثة لتعكس بيئة VPS الجديدة
> - ✅ جميع بيانات الاتصال بالسيرفر وقاعدة البيانات محدثة في هذا الملف
> - ❌ لا تستخدم أي معلومات قديمة متعلقة بـ Hostinger Shared Hosting
>
> #### 5. **الالتزام إلزامي**
> - الالتزام بهذه القواعد إلزامي لضمان توحيد العمل وسهولة الصيانة
> - أي محاولة لإنشاء ملفات `.md` أو `.sql` إضافية ستُرفض وتُحذف تلقائياً

---

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#-نظرة-عامة-على-المشروع)
2. [الروابط المهمة](#-الروابط-المهمة)
3. [التقنيات المستخدمة](#-التقنيات-المستخدمة)
4. [البدء السريع](#-البدء-السريع)
5. [التثبيت والإعداد المحلي](#-التثبيت-والإعداد-المحلي)
6. [بيئة الإنتاج (VPS)](#-بيئة-الإنتاج-vps)
7. [قاعدة البيانات (VPS)](#-قاعدة-البيانات-vps)
8. [النشر والتحديث](#-النشر-والتحديث)
9. [إدارة الخدمة على الخادم](#-إدارة-الخدمة-على-الخادم)
10. [إعدادات خادم الويب (Nginx)](#-إعدادات-خادم-الويب-nginx)
11. [إدارة اللوجوهات والمحتوى المرئي](#-إدارة-اللوجوهات-والمحتوى-المرئي)
12. [نظام المصادقة ولوحة التحكم](#-نظام-المصادقة-ولوحة-التحكم)
13. [سير العمل الموصى به](#-سير-العمل-الموصى-به)
14. [أخطاء شائعة يجب تجنبها](#-أخطاء-شائعة-يجب-تجنبها)
15. [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)
16. [تقرير إصلاح نموذج التواصل](#-تقرير-إصلاح-نموذج-التواصل)
17. [قائمة التحقق قبل كل Push](#-قائمة-التحقق-قبل-كل-push)
18. [الترخيص](#-الترخيص)

---

## 🏢 نظرة عامة على المشروع

موقع إلكتروني احترافي لشركة **نسمة برزان التجارية**، مع لوحة تحكم كاملة لإدارة المحتوى، يعمل على خادم VPS مخصص لضمان أفضل أداء وأمان.

### المميزات الرئيسية

**الموقع الرئيسي:**
- تصميم احترافي متجاوب يعمل على جميع الأجهزة
- دعم ثنائي اللغة (عربي/إنجليزي) مع تبديل سلس
- عرض احترافي للمشاريع والخدمات
- معرض صور تفاعلي مع إمكانية التكبير
- نموذج تواصل متقدم مع التحقق من البيانات
- تحسين محركات البحث (SEO)

**لوحة التحكم:**
- نظام دخول آمن مع تشفير كلمات المرور
- إدارة كاملة للمحتوى (نصوص، مشاريع، صور، إعدادات)
- إدارة الرسائل الواردة من نموذج التواصل
- تغيير كلمة المرور بشكل آمن
- واجهة سهلة الاستخدام باللغة العربية

**البنية التحتية:**
- خادم VPS مخصص لأداء عالي وتحكم كامل
- قاعدة بيانات MySQL محسّنة
- خدمة Node.js دائمة باستخدام systemd
- Nginx كـ Reverse Proxy
- نظام نشر تلقائي (Auto-Deploy)

---

## 🔗 الروابط المهمة

| الوصف | الرابط |
| :--- | :--- |
| **الموقع الرئيسي** | `https://www.shheer.com` |
| **لوحة التحكم** | `https://www.shheer.com/admin` |
| **مستودع GitHub** | `https://github.com/DROPIDEA0/NESMA-BARAZAN` |
| **لوحة إدارة السيرفر** | `https://resq.click/` |

---

## 🛠️ التقنيات المستخدمة

| المجال | التقنية | الإصدار |
| :--- | :--- | :--- |
| **Frontend** | React | `19.2.1` |
| | TypeScript | `5.9.3` |
| | Vite | `7.1.7` |
| | TailwindCSS | `4.1.14` |
| **Backend** | Node.js | `22.x` |
| | Express | `4.21.2` |
| | tRPC | `11.6.0` |
| | Drizzle ORM | `0.45.1` |
| **Database** | MySQL | `8.4.6` |
| **Infrastructure** | Ubuntu | `22.04` |
| | Nginx | `1.28.0` |
| | systemd | System Service Manager |

---

## 🚀 البدء السريع

```bash
# استنساخ المشروع من GitHub
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# تثبيت الاعتماديات باستخدام pnpm
pnpm install

# تشغيل وضع التطوير
pnpm run dev
```

الموقع سيعمل على `http://localhost:3000`

---

## 🚀 التثبيت والإعداد المحلي

### المتطلبات الأساسية

- **Node.js:** الإصدار 18 أو أحدث
- **pnpm:** مدير الحزم (يمكن تثبيته عبر `npm install -g pnpm`)
- **MySQL:** قاعدة بيانات محلية للتطوير (الإصدار 8.0 أو أحدث)

### خطوات التثبيت

```bash
# 1. استنساخ المشروع
git clone https://github.com/DROPIDEA0/NESMA-BARAZAN.git
cd NESMA-BARAZAN

# 2. تثبيت الاعتماديات
pnpm install

# 3. إعداد ملف .env
# انسخ ملف .env.production وعدّل بيانات قاعدة البيانات المحلية
cp .env.production .env

# 4. إنشاء قاعدة بيانات محلية واستيراد البنية
mysql -u root -p
CREATE DATABASE nesma_barzan_local;
exit;
mysql -u root -p nesma_barzan_local < DATABASE.sql

# 5. تشغيل وضع التطوير
pnpm run dev
```

### ملف .env للتطوير المحلي

```env
DATABASE_URL="mysql://root:password@localhost:3306/nesma_barzan_local"
NODE_ENV=development
PORT=3000
```

**ملاحظة:** استبدل `root` و `password` ببيانات قاعدة البيانات المحلية الخاصة بك.

---

## 🌐 بيئة الإنتاج (VPS)

### معلومات الخادم

| المعلومة | القيمة |
| :--- | :--- |
| **نوع الاستضافة** | VPS مخصص |
| **عنوان IP** | `72.62.7.159` |
| **نظام التشغيل** | Ubuntu 22.04 LTS |
| **مسار المشروع** | `/home/shheercom/htdocs/www.shheer.com` |
| **اسم الخدمة** | `nesma-barzan.service` |

### بيانات الدخول للخادم (SSH)

```bash
# الاتصال بالخادم عبر SSH
ssh root@72.62.7.159

# كلمة المرور
Downy144168@#144168
```

**ملاحظة أمنية:** يُنصح بإعداد مفاتيح SSH بدلاً من استخدام كلمة المرور المباشرة.

### بيانات الدخول للوحة إدارة السيرفر

| المعلومة | القيمة |
| :--- | :--- |
| **الرابط** | `https://resq.click/` |
| **اسم المستخدم** | `admin` |
| **كلمة المرور** | `Downy144168@#5519971930` |

---

## 🗄️ قاعدة البيانات (VPS)

### بيانات الاتصال

| المعلومة | القيمة |
| :--- | :--- |
| **Host** | `127.0.0.1` |
| **Port** | `3306` |
| **اسم قاعدة البيانات** | `u521934522-nasma-db` |
| **اسم المستخدم** | `u521934522-nasma-db-new` |
| **كلمة المرور** | `Downy144168@144168` |

### ملف .env على الخادم

يجب أن يحتوي ملف `.env` على الخادم على المتغيرات التالية:

```env
DATABASE_URL="mysql://u521934522-nasma-db-new:Downy144168@144168@127.0.0.1:3306/u521934522-nasma-db"
NODE_ENV=production
PORT=3000
```

**موقع الملف:** `/home/shheercom/htdocs/www.shheer.com/.env`

### جداول قاعدة البيانات

قاعدة البيانات تحتوي على الجداول التالية:

| الجدول | الوصف |
| :--- | :--- |
| `contact_messages` | رسائل التواصل من الزوار |
| `features` | الميزات والخدمات المعروضة |
| `hero_stats` | إحصائيات القسم الرئيسي |
| `images` | معرض الصور |
| `navigation_items` | عناصر القائمة |
| `projects` | المشاريع المنفذة |
| `site_content` | محتوى الموقع (نصوص، عناوين) |
| `site_settings` | إعدادات الموقع العامة |
| `users` | مستخدمي لوحة التحكم |

### تحديث قاعدة البيانات

#### استيراد قاعدة البيانات من ملف DATABASE.sql

```bash
# الاتصال بالخادم
ssh root@72.62.7.159

# الانتقال إلى مسار المشروع
cd /home/shheercom/htdocs/www.shheer.com

# استيراد قاعدة البيانات
mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db < DATABASE.sql
```

#### تصدير قاعدة البيانات (للتحديث على GitHub)

```bash
# الاتصال بالخادم
ssh root@72.62.7.159

# تصدير قاعدة البيانات
mysqldump -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db > /home/shheercom/htdocs/www.shheer.com/DATABASE.sql

# رفع التحديث إلى GitHub
cd /home/shheercom/htdocs/www.shheer.com
git add DATABASE.sql
git commit -m "تحديث قاعدة البيانات"
git push origin main
```

### الاتصال بقاعدة البيانات مباشرة

```bash
# الاتصال بالخادم
ssh root@72.62.7.159

# الدخول إلى MySQL
mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db

# عرض الجداول
SHOW TABLES;

# الخروج
exit;
```

---

## 🔄 النشر والتحديث

تم إعداد سكريبت `deploy.sh` على الخادم لأتمتة عملية النشر بالكامل.

### طريقة النشر التلقائي

#### الخطوات:

1. قم بعمل التعديلات المطلوبة على الكود محلياً
2. اختبر التعديلات في بيئة التطوير
3. قم بعمل `commit` و `push` للتحديثات إلى فرع `main` في GitHub
4. اتصل بالخادم وقم بتشغيل سكريبت النشر

```bash
# على جهازك المحلي
git add .
git commit -m "وصف التحديث"
git push origin main

# على الخادم
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

### محتوى سكريبت deploy.sh

```bash
#!/bin/bash

echo "🚀 بدء عملية النشر التلقائي..."

# سحب آخر التحديثات من GitHub
echo "📥 سحب التحديثات من GitHub..."
git pull origin main

# تثبيت الاعتماديات
echo "📦 تثبيت الاعتماديات..."
pnpm install

# بناء المشروع
echo "🔨 بناء المشروع..."
pnpm run build

# إعادة تشغيل الخدمة
echo "🔄 إعادة تشغيل الخدمة..."
systemctl restart nesma-barzan.service

# عرض حالة الخدمة
echo "✅ تم النشر بنجاح!"
systemctl status nesma-barzan.service --no-pager
```

### التحقق من نجاح النشر

```bash
# عرض حالة الخدمة
systemctl status nesma-barzan.service

# عرض آخر 20 سطر من السجلات
journalctl -u nesma-barzan.service -n 20 --no-pager

# متابعة السجلات مباشرة
journalctl -u nesma-barzan.service -f --no-pager
```

---

## ⚙️ إدارة الخدمة على الخادم

المشروع يعمل كخدمة دائمة باستخدام `systemd`.

### معلومات الخدمة

- **اسم الخدمة:** `nesma-barzan.service`
- **ملف الخدمة:** `/etc/systemd/system/nesma-barzan.service`
- **المستخدم:** `root`
- **مسار العمل:** `/home/shheercom/htdocs/www.shheer.com`

### أوامر التحكم في الخدمة

```bash
# عرض حالة الخدمة
systemctl status nesma-barzan.service

# إعادة تشغيل الخدمة (بعد التحديث)
systemctl restart nesma-barzan.service

# إيقاف الخدمة
systemctl stop nesma-barzan.service

# تشغيل الخدمة
systemctl start nesma-barzan.service

# تفعيل الخدمة للتشغيل التلقائي عند بدء النظام
systemctl enable nesma-barzan.service

# إلغاء تفعيل التشغيل التلقائي
systemctl disable nesma-barzan.service
```

### عرض السجلات (Logs)

```bash
# عرض آخر 50 سطر من السجلات
journalctl -u nesma-barzan.service -n 50 --no-pager

# متابعة السجلات مباشرة (real-time)
journalctl -u nesma-barzan.service -f --no-pager

# عرض سجلات اليوم فقط
journalctl -u nesma-barzan.service --since today --no-pager

# عرض سجلات آخر ساعة
journalctl -u nesma-barzan.service --since "1 hour ago" --no-pager
```

### محتوى ملف nesma-barzan.service

```ini
[Unit]
Description=Nesma Barzan Trading Website
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/home/shheercom/htdocs/www.shheer.com
ExecStart=/usr/bin/node /home/shheercom/htdocs/www.shheer.com/dist/index.js
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### تعديل ملف الخدمة

إذا احتجت لتعديل ملف الخدمة:

```bash
# تحرير ملف الخدمة
nano /etc/systemd/system/nesma-barzan.service

# إعادة تحميل ملفات systemd
systemctl daemon-reload

# إعادة تشغيل الخدمة
systemctl restart nesma-barzan.service
```

---

## 🌐 إعدادات خادم الويب (Nginx)

Nginx يعمل كـ **Reverse Proxy** لتوجيه الطلبات من المنفذ 80/443 إلى خدمة Node.js على المنفذ 3000.

### معلومات الإعداد

- **ملف الإعداد:** `/etc/nginx/sites-enabled/www.shheer.com.conf`
- **المنفذ الخارجي:** `80` (HTTP)
- **المنفذ الداخلي:** `3000` (Node.js)

### أوامر التحكم في Nginx

```bash
# اختبار صحة الإعدادات
nginx -t

# إعادة تحميل Nginx (بعد تعديل الإعدادات)
systemctl reload nginx

# إعادة تشغيل Nginx
systemctl restart nginx

# عرض حالة Nginx
systemctl status nginx

# إيقاف Nginx
systemctl stop nginx

# تشغيل Nginx
systemctl start nginx
```

### محتوى ملف www.shheer.com.conf

```nginx
server {
    listen 80;
    server_name www.shheer.com shheer.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### سجلات Nginx

```bash
# سجلات الأخطاء
tail -f /var/log/nginx/error.log

# سجلات الوصول
tail -f /var/log/nginx/access.log
```

### تعديل إعدادات Nginx

```bash
# تحرير ملف الإعداد
nano /etc/nginx/sites-enabled/www.shheer.com.conf

# اختبار الإعدادات
nginx -t

# إعادة تحميل Nginx
systemctl reload nginx
```

---

## 🎨 إدارة اللوجوهات والمحتوى المرئي

### نظام اللوجوهات الثلاثة

تم تنظيم إدارة اللوجوهات في الموقع بحيث يكون هناك ثلاثة لوجوهات مستقلة:

#### 1. لوجو Header/Footer + Favicon

- **الموقع:** صفحة الإعدادات (`/admin/settings`)
- **الوصف:** اللوجو الذي يظهر في رأس وتذييل الموقع، بالإضافة إلى أيقونة الموقع (Favicon)
- **طريقة الرفع:** رفع صورة مباشر من صفحة الإعدادات
- **الحقول في قاعدة البيانات:** `site_settings.site_logo` و `site_settings.site_favicon`

#### 2. اللوجو الثاني (Hero Section)

- **الموقع:** صفحة إدارة المحتوى (`/admin/content`) → تبويب "القسم الرئيسي"
- **الوصف:** اللوجو الكبير الذي يظهر في القسم الرئيسي (Hero) من الصفحة الرئيسية
- **طريقة الرفع:** رفع صورة مباشر مع معاينة فورية
- **الحقل في قاعدة البيانات:** `site_content.hero_logo`
- **التسمية:** "اللوجو الثاني" (عربي) / "Second Logo" (إنجليزي)

#### 3. لوجو البري لود (Preloader)

- **الموقع:** صفحة الإعدادات (`/admin/settings`)
- **الوصف:** اللوجو الذي يظهر أثناء تحميل الموقع (شاشة التحميل الأولية)
- **طريقة الرفع:** رفع صورة مباشر من صفحة الإعدادات
- **الحقل في قاعدة البيانات:** `site_settings.preloader_logo`

### إدارة معرض الصور

- **الموقع:** صفحة إدارة المحتوى (`/admin/content`) → تبويب "معرض الصور"
- **الميزات:**
  - رفع صور متعددة دفعة واحدة
  - معاينة الصور قبل الرفع
  - إعادة ترتيب الصور بالسحب والإفلات
  - حذف الصور
  - تحديث العنوان والوصف لكل صورة

### مسار تخزين الملفات المرفوعة

- **المسار على الخادم:** `/home/shheercom/htdocs/www.shheer.com/uploads/`
- **الصلاحيات المطلوبة:** `777` (للقراءة والكتابة)

```bash
# التأكد من صلاحيات مجلد uploads
chmod -R 777 /home/shheercom/htdocs/www.shheer.com/uploads
```

---

## 🔐 نظام المصادقة ولوحة التحكم

### بيانات الدخول للوحة التحكم

| المعلومة | القيمة |
| :--- | :--- |
| **الرابط** | `https://www.shheer.com/admin` |
| **اسم المستخدم** | `admin` |
| **كلمة المرور** | `Bandar1#%` |

### أقسام لوحة التحكم

1. **لوحة المعلومات (Dashboard):** إحصائيات سريعة عن الموقع
2. **إدارة المحتوى:** تحرير نصوص الموقع، المشاريع، معرض الصور
3. **إدارة الرسائل:** عرض وإدارة رسائل التواصل الواردة
4. **الإعدادات:** إعدادات الموقع العامة، اللوجوهات، معلومات الاتصال
5. **تغيير كلمة المرور:** تغيير كلمة مرور المستخدم الحالي

### تغيير كلمة المرور

```bash
# الطريقة الأولى: من لوحة التحكم
1. سجل الدخول إلى لوحة التحكم
2. اذهب إلى صفحة "تغيير كلمة المرور"
3. أدخل كلمة المرور الحالية والجديدة
4. احفظ التغييرات

# الطريقة الثانية: من قاعدة البيانات
ssh root@72.62.7.159
mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db

# تحديث كلمة المرور (مشفرة بـ bcrypt)
UPDATE users SET password = '$2b$10$...' WHERE username = 'admin';
exit;
```

**ملاحظة:** كلمات المرور مشفرة باستخدام bcrypt. لا تقم بتخزين كلمات المرور بشكل نصي.

---

## 🔄 سير العمل الموصى به

### عند إضافة ميزة جديدة

1. ✅ اعمل على فرع منفصل (branch) للتطوير
2. ✅ اختبر الميزة محلياً قبل الـ Push
3. ✅ حدّث `DOCUMENTATION.md` بالمعلومات الجديدة
4. ✅ احذف أي ملفات `.md` أو `.sql` إضافية
5. ✅ اعمل Push للفرع
6. ✅ اعمل Merge للفرع الرئيسي (main)
7. ✅ نفذ سكريبت `deploy.sh` على السيرفر

```bash
# على جهازك المحلي
git checkout -b feature/new-feature
# ... عمل التعديلات
git add .
git commit -m "إضافة ميزة جديدة"
git push origin feature/new-feature
# ... عمل Pull Request و Merge

# على السيرفر
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

### عند إصلاح مشكلة

1. ✅ شخّص المشكلة من السجلات (logs)
2. ✅ أصلح المشكلة في الكود
3. ✅ اختبر الإصلاح محلياً
4. ✅ حدّث التوثيق إذا لزم الأمر
5. ✅ اعمل Push
6. ✅ نفذ سكريبت `deploy.sh` على السيرفر

```bash
# فحص السجلات
ssh root@72.62.7.159
journalctl -u nesma-barzan.service -n 50 --no-pager

# بعد الإصلاح
git add .
git commit -m "إصلاح: وصف المشكلة"
git push origin main

# النشر
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

### عند تحديث قاعدة البيانات

1. ✅ عدّل البنية محلياً أو على السيرفر
2. ✅ صدّر قاعدة البيانات الكاملة من السيرفر
3. ✅ استبدل محتوى `DATABASE.sql`
4. ✅ حدّث التوثيق بالتغييرات
5. ✅ اعمل Push
6. ✅ نفذ سكريبت `deploy.sh` على السيرفر

```bash
# تصدير قاعدة البيانات
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
mysqldump -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db > DATABASE.sql

# رفع التحديث
git add DATABASE.sql DOCUMENTATION.md
git commit -m "تحديث بنية قاعدة البيانات"
git push origin main
```

---

## ❌ أخطاء شائعة يجب تجنبها

### 1. إنشاء ملفات توثيق إضافية

```bash
# ❌ خطأ
touch README.md
touch DEPLOYMENT.md
touch TODO.md
touch DEVELOPER_GUIDELINES.md

# ✅ صحيح
# عدّل DOCUMENTATION.md فقط
nano DOCUMENTATION.md
```

### 2. رفع ملفات SQL إضافية

```bash
# ❌ خطأ
git add migration_v2.sql
git add backup_20260104.sql
git add schema.sql

# ✅ صحيح
# حدّث DATABASE.sql فقط
mysqldump ... > DATABASE.sql
git add DATABASE.sql
```

### 3. النشر بدون سكريبت deploy.sh

```bash
# ❌ خطأ
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
git pull
pnpm install
pnpm run build
# نسيت إعادة تشغيل الخدمة!

# ✅ صحيح
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

### 4. استخدام معلومات قديمة

```bash
# ❌ خطأ - استخدام معلومات Hostinger Shared Hosting القديمة
# ❌ خطأ - استخدام كلمة مرور قديمة
# ❌ خطأ - استخدام مسار قديم

# ✅ صحيح
# استخدم المعلومات من DOCUMENTATION.md فقط
# IP: 72.62.7.159
# المسار: /home/shheercom/htdocs/www.shheer.com
```

### 5. تعديل الملفات مباشرة على السيرفر

```bash
# ❌ خطأ
ssh root@72.62.7.159
nano /home/shheercom/htdocs/www.shheer.com/server/index.ts
# التعديلات لن تُحفظ في GitHub وستُفقد عند النشر التالي

# ✅ صحيح
# عدّل محلياً، ثم Push، ثم deploy
```

---

## 🔧 حل المشاكل الشائعة

### الموقع لا يعمل

```bash
# 1. تحقق من حالة الخدمة
systemctl status nesma-barzan.service

# 2. إذا كانت متوقفة، شغّلها
systemctl start nesma-barzan.service

# 3. إذا كانت لا تعمل، افحص السجلات
journalctl -u nesma-barzan.service -n 50 --no-pager

# 4. أعد تشغيل الخدمة
systemctl restart nesma-barzan.service
```

### التحديثات لا تظهر

```bash
# 1. تأكد من أنك قمت بتشغيل deploy.sh بعد الـ Push
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh

# 2. امسح الكاش في متصفحك
# Ctrl + Shift + R (Windows/Linux)
# Cmd + Shift + R (Mac)

# 3. تحقق من أن البناء تم بنجاح
journalctl -u nesma-barzan.service -n 20 --no-pager
```

### مشاكل قاعدة البيانات

```bash
# 1. تحقق من صحة بيانات الاتصال في ملف .env
cat /home/shheercom/htdocs/www.shheer.com/.env

# 2. تأكد من أن خدمة MySQL تعمل
systemctl status mysql

# 3. اختبر الاتصال بقاعدة البيانات
mysql -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db

# 4. إذا فشل الاتصال، أعد تشغيل MySQL
systemctl restart mysql
```

### مشاكل الصلاحيات

```bash
# 1. تأكد من صلاحيات مجلد المشروع
chown -R root:root /home/shheercom/htdocs/www.shheer.com
chmod -R 755 /home/shheercom/htdocs/www.shheer.com

# 2. تأكد من صلاحيات مجلد uploads
chmod -R 777 /home/shheercom/htdocs/www.shheer.com/uploads

# 3. تأكد من صلاحيات ملف .env
chmod 600 /home/shheercom/htdocs/www.shheer.com/.env
```

### خطأ في المنفذ (Port Already in Use)

```bash
# 1. ابحث عن العملية التي تستخدم المنفذ 3000
lsof -i :3000

# 2. أوقف العملية
kill -9 <PID>

# 3. أعد تشغيل الخدمة
systemctl restart nesma-barzan.service
```

### مشاكل Nginx

```bash
# 1. تحقق من حالة Nginx
systemctl status nginx

# 2. اختبر صحة الإعدادات
nginx -t

# 3. افحص سجلات الأخطاء
tail -f /var/log/nginx/error.log

# 4. أعد تشغيل Nginx
systemctl restart nginx
```

---

## 🔧 تقرير إصلاح نموذج التواصل

**التاريخ:** 02 يناير 2026  
**الحالة:** ✅ تم الإصلاح بنجاح

### ملخص تنفيذي

تم إصلاح مشكلة نموذج التواصل بنجاح بعد تشخيص دقيق للمشكلة الجذرية. المشكلة كانت في استخدام Drizzle ORM wrapper بدلاً من raw MySQL connection لتنفيذ استعلامات SQL المباشرة.

### المشكلة الأصلية

#### الأعراض

- نموذج التواصل في صفحة "تواصل معنا" لا يرسل البيانات
- تظهر رسالة فشل عملية الإرسال للمستخدم
- الرسائل لا تظهر في لوحة التحكم (إدارة الرسائل)

#### التشخيص الأولي

1. الخدمة كانت تتعطل باستمرار بسبب تعارض المنفذ 3000
2. عند حل مشكلة المنفذ، ظهرت مشكلة SQL في دوال contact messages

### الحل المطبق

#### المشكلة الجذرية

كان الكود يستخدم `db.execute()` من Drizzle ORM لتنفيذ raw SQL queries. المشكلة أن Drizzle يعترض هذه الاستدعاءات ولا يمرر المعاملات (parameters) بشكل صحيح، مما يسبب خطأ SQL:

```
Error: You have an error in your SQL syntax near '? OFFSET ?'
```

#### الحل النهائي

إنشاء `rawConnection` منفصلة للـ raw SQL queries:

##### 1. تعديل `server/db-mysql.ts`

```typescript
// إضافة rawConnection منفصلة
let rawConnection: mysql.Connection | null = null;

// في initializeMySQL()
connection = await mysql.createConnection(config);
rawConnection = await mysql.createConnection(config); // اتصال منفصل للـ raw SQL
db = drizzle(connection); // Drizzle يستخدم connection فقط

// دالة للحصول على raw connection
export async function getMySQLConnection() {
  if (!rawConnection) {
    await initializeMySQL();
  }
  return rawConnection; // تُرجع raw connection وليس Drizzle instance
}
```

##### 2. تعديل `server/db.ts`

```typescript
// استيراد getMySQLConnection
import { getMySQLConnection } from './db-mysql';

// تعديل جميع دوال contact messages
export async function createContactMessage(data) {
  const conn = await getMySQLConnection(); // بدلاً من getDb()
  const query = 'INSERT INTO contact_messages ...';
  const [result] = await conn.query(query, [...]); // query بدلاً من execute
  return result.insertId;
}

export async function getContactMessages(options) {
  const conn = await getMySQLConnection();
  let query = 'SELECT * FROM contact_messages';
  const params = [];
  // ... بناء الاستعلام
  const [rows] = await conn.query(query, params);
  return rows;
}

// نفس الأسلوب لجميع دوال contact messages:
// - getContactMessageById
// - updateContactMessageStatus
// - deleteContactMessage
// - getContactMessageCounts
```

### النتائج

#### الاختبارات الناجحة

1. ✅ **إرسال رسالة من نموذج التواصل:** نجح
2. ✅ **حفظ الرسالة في قاعدة البيانات:** نجح
3. ✅ **عرض الرسائل في لوحة التحكم:** نجح
4. ✅ **عرض الإحصائيات (جديد، مقروء، مُجاب):** نجح

#### البيانات المختبرة

```
الاسم: أحمد محمد الاختبار النهائي
البريد: finaltest@example.com
الهاتف: +966555123456
الموضوع: اختبار نموذج التواصل النهائي
الرسالة: هذه رسالة اختبار نهائية...
الحالة: new
التاريخ: 2026-01-02 11:57:10
```

### التعديلات المطبقة

#### الملفات المعدلة

1. **server/db-mysql.ts**
   - إضافة `rawConnection` منفصلة
   - تعديل `getMySQLConnection()` لإرجاع raw connection

2. **server/db.ts**
   - استيراد `getMySQLConnection`
   - تعديل 5 دوال contact messages لاستخدام raw connection
   - تغيير `execute()` إلى `query()` في جميع الدوال

### الدروس المستفادة

#### المشكلة الرئيسية

عند استخدام Drizzle ORM، لا يمكن استخدام `db.execute()` لتنفيذ raw SQL queries مع parameters. يجب استخدام raw MySQL connection مباشرة.

#### الحل الأمثل

إنشاء connection منفصلة للـ raw SQL queries بدلاً من استخدام نفس الـ connection المُمررة لـ Drizzle.

#### التوصيات المستقبلية

1. **إضافة جدول contact_messages إلى Drizzle schema** لاستخدام ORM بدلاً من raw SQL
2. **إعداد SSL Certificate (HTTPS)** للموقع
3. **تغيير كلمة مرور لوحة التحكم** من القيمة الافتراضية
4. **إعداد نظام النسخ الاحتياطي التلقائي** لقاعدة البيانات

---

## ✅ قائمة التحقق قبل كل Push

قبل رفع أي تحديثات إلى GitHub، تأكد من:

- [ ] هل حدّثت `DOCUMENTATION.md` بالمعلومات الجديدة؟
- [ ] هل حذفت أي ملفات `.md` إضافية؟
- [ ] هل حذفت أي ملفات `.sql` إضافية (ما عدا DATABASE.sql)؟
- [ ] هل اختبرت التحديثات محلياً؟
- [ ] هل تأكدت من عدم وجود أخطاء في الكود؟
- [ ] هل كتبت رسالة commit واضحة ووصفية؟
- [ ] هل تأكدت من عدم رفع ملفات `.env` أو بيانات حساسة؟
- [ ] هل راجعت التغييرات قبل الـ Push؟

```bash
# قبل Push، تحقق من الملفات
git status

# تأكد من عدم وجود ملفات MD إضافية
find . -name "*.md" -type f | grep -v DOCUMENTATION.md

# تأكد من عدم وجود ملفات SQL إضافية
find . -name "*.sql" -type f | grep -v DATABASE.sql

# إذا وجدت ملفات إضافية، احذفها
git rm <filename>
```

---

## 🎯 الهدف من هذه الإرشادات

### الحفاظ على:

- ✅ توحيد التوثيق في مكان واحد
- ✅ سهولة الصيانة والتحديث
- ✅ تجنب التكرار والفوضى
- ✅ ضمان استخدام المعلومات الأحدث دائماً
- ✅ تسهيل عمل الفريق والوكلاء
- ✅ توحيد سير العمل والنشر

### تجنب:

- ❌ تشتت المعلومات في ملفات متعددة
- ❌ استخدام معلومات قديمة أو غير صحيحة
- ❌ صعوبة تتبع التحديثات
- ❌ الأخطاء الناتجة عن عدم التوحيد
- ❌ تعارض المعلومات بين الملفات المختلفة

---

## 📝 الترخيص

هذا المشروع ملك لشركة **نسمة برزان التجارية**. جميع الحقوق محفوظة © 2025-2026.

---

## 📞 الدعم والتواصل

للحصول على الدعم الفني أو الإبلاغ عن مشاكل:

- **الموقع الرسمي:** https://www.shheer.com
- **مستودع GitHub:** https://github.com/DROPIDEA0/NESMA-BARAZAN
- **البريد الإلكتروني:** (يمكن إضافته من لوحة التحكم)

---

## 📊 معلومات إضافية

### إحصائيات المشروع

- **تاريخ البدء:** 2025
- **آخر تحديث:** 04 يناير 2026
- **الإصدار الحالي:** 4.0.0
- **حالة المشروع:** ✅ نشط وجاهز للإنتاج

### التحديثات القادمة

1. إعداد SSL Certificate (HTTPS)
2. تحسين أداء الموقع
3. إضافة نظام النسخ الاحتياطي التلقائي
4. تحسين نظام إدارة الصور
5. إضافة ميزات جديدة للوحة التحكم

---

**تذكر:** هذه الإرشادات إلزامية وليست اختيارية. الالتزام بها يضمن سير العمل بسلاسة وكفاءة.

---

**آخر تحديث:** 04 يناير 2026 | **الإصدار:** 4.0.0 | **الحالة:** ✅ جاهز للإنتاج
